# Cronómetro de Reacción para Modelos Web

## Instalación

### En Windows
```bash
pip install -r requirements.txt --break-system-packages

# Desinstalar versión anterior si existe
pip uninstall pynput --break-system-packages 

# Instalar la versión de desarrollo (compatible con Python 3.13+)
pip install git+https://github.com/moses-palmer/pynput.git --break-system-packages
